{
  "dependencies": {
    "firebase": "^10.5.2",
    "firebase-tools": "^13.35.1"
  },
  "scripts": {
    "lint": "cd functions && npm run lint --fix",
    "build": "cd functions && npm run build",
    "build:debug": "cd functions && npm run build:debug",
    "build:watch": "cd functions && npm run build:watch",
    "start": "firebase emulators:start",
    "start:import": "firebase emulators:start --inspect-functions --import=\"./resource\""
  },
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3"
  }
}
